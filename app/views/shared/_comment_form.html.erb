<%= form_for @comment, url: comment_path, remote: true do |form| %>
  <div class="form-group">
  	<%= form.text_area :text, placeholder: "comment", class: "form-control" %>
  </div>
  <%= form.submit "Comment", class: "btn btn-primary btn-sm" %>
<% end %>